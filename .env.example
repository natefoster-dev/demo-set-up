# Core (clone-from) account — required for demo-init cloner step
CORE_CLIENT_ID=
CORE_CLIENT_SECRET=

# Demo (target) account — optional here; demo-init prompts for these after signup.
# For standalone `node cloner.js`, set them in this file.
DEMO_CLIENT_ID=
DEMO_CLIENT_SECRET=
